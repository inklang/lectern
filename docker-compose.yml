services:
  lectern:
    build: .
    image: ghcr.io/inklang/lectern:latest
    restart: unless-stopped
    ports:
      - "172.17.0.1:4321:4321"
    volumes:
      - lectern-storage:/data
    environment:
      BASE_URL: ${BASE_URL:-https://lectern.inklang.org}
      LECTERN_TOKENS: ${LECTERN_TOKENS}
      STORAGE_DIR: /data

volumes:
  lectern-storage:
